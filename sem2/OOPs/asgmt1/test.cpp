#include <iostream>
using namespace std;

void solve() {
	int n;
	cin >> n;

	for (int a = 2; a < = n/4; a++) {
		for (int b = 2; b <= n/2; b++) {
			int c = n / (a * b);
		}
	}
}

int main() {
	cout.tie(0);
	cin.tie(0);

	int t;
	cin >> t;

	while (t--) {
		solve();
	}
}
